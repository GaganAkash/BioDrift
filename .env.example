# Copy to .env and fill in to lock the web console behind a token.
# With BIODRIFT_API_TOKEN set and ALLOW_ANON_GET=0, every write and every
# read-through the API requires `Authorization: Bearer <token>`.
BIODRIFT_API_TOKEN=
BIODRIFT_ALLOW_ANON_GET=1

# Web server
WEB_HOST=0.0.0.0
WEB_PORT=8000
